動作：
    SpeedX,Yの上限を決定する。
    下からバーとボールが衝突したときはボールは下に跳ね返る
    終了ボタン的な
    ボールの位置に応じてスクリーンが上下する
    バーを動かす
    バーを複数かする

リファクタリング:
    フォルダ構成の整理
    テストをforでまとめる
    オブジェクトのスーパクラスで処理をまとめる

その他:
    vimの日本語入力環境を整理する

==complete======================================
動作：
    バーを表示する
    バーとボールが衝突する
    バーとボールが衝突した後ボールがはねる
    warpオブジェクトの実装
リファクタリング:
    マジックナンバーの消去
